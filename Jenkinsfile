pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "pipeline task"
            }
        }
        stage('Test') {
            steps {
                echo "pipeline project"
            }
        }
     
    }
}
